/*
 * This is an empty Map holding the spell assignments.
 * It will be loaded from local storage at startup,
 * or if it isn't there, then it will be
 * auto generated at startup based on the order of
 * playerObject.spellOptions.
 */
export default new Map();
